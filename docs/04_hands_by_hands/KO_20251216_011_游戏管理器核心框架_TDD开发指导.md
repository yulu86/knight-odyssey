# 游戏管理器核心框架 TDD 开发指导

**Story ID**: KO_20251216_011
**Story 标题**: 游戏管理器核心框架
**开发日期**: 2025-12-21
**预估工作量**: 4天（8个Story点）

---

## 📋 目录

1. [开发概述](#开发概述)
2. [架构设计](#架构设计)
3. [开发环境准备](#开发环境准备)
4. [TDD开发步骤](#tdd开发步骤)
5. [测试实现指导](#测试实现指导)
6. [代码实现指导](#代码实现指导)
7. [集成测试](#集成测试)
8. [部署配置](#部署配置)
9. [验收检查清单](#验收检查清单)

---

## 开发概述

### 🎯 开发目标
建立统一的游戏管理框架，协调各个系统之间的交互和管理游戏状态，为后续功能开发提供稳定的基础架构。

### ✨ 核心功能
- GameManager：游戏主管理器，协调各子系统
- EventBus：事件总线，处理系统间通信
- ConfigManager：配置管理器（增强现有实现）
- SceneManager：场景管理器，处理场景切换
- SaveManager：存档管理器，处理游戏进度保存

### 🔧 技术要求
- 使用AutoLoad单例模式
- 系统间通过EventBus解耦
- 支持配置参数外部化
- TDD测试驱动开发
- 代码覆盖率：单元测试70%，集成测试20%，端到端测试10%

---

## 架构设计

### 🏗️ 整体架构图

```
GameManager (主协调器)
    ├── 初始化所有管理器
    ├── 管理游戏生命周期
    └── 协调系统交互

EventBus (事件通信总线)
    ├── 事件订阅/分发
    ├── 异步事件支持
    └── 事件优先级管理

ConfigManager (配置管理)
    ├── 配置文件读写
    ├── 配置验证
    └── 热重载支持

SceneManager (场景管理)
    ├── 场景加载/卸载
    ├── 加载进度管理
    └── 场景堆栈管理

SaveManager (存档管理)
    ├── 存档文件管理
    ├── 数据序列化
    └── 自动保存
```

### 🔄 数据流设计

```
游戏启动
    ↓
GameManager.initialize()
    ↓
EventBus ←→ 各管理器（通过事件通信）
    ↓
ConfigManager 加载配置
    ↓
SceneManager 加载初始场景
    ↓
SaveManager 准备存档系统
```

---

## 开发环境准备

### 📁 目录结构创建

```
scripts/managers/          # 管理器脚本目录
├── game_manager.gd       # 游戏主管理器
├── event_bus.gd          # 事件总线
├── config_manager.gd     # 配置管理器
├── scene_manager.gd      # 场景管理器
└── save_manager.gd       # 存档管理器

test/                     # 测试目录
├── unit/                # 单元测试
│   ├── test_game_manager.gd
│   ├── test_event_bus.gd
│   ├── test_config_manager.gd
│   ├── test_scene_manager.gd
│   └── test_save_manager.gd
├── integration/         # 集成测试
│   ├── test_manager_coordination.gd
│   └── test_event_flow.gd
└── e2e/                # 端到端测试
    └── test_game_lifecycle.gd

configs/                # 配置文件目录
└── game_config.cfg     # 游戏配置
```

### 🛠️ AutoLoad配置

在 Godot 编辑器中：
1. 打开 项目 → 项目设置 → AutoLoad
2. 添加以下单例：
   - GameManager: `res://scripts/managers/game_manager.gd`
   - EventBus: `res://scripts/managers/event_bus.gd`
   - ConfigManager: `res://scripts/managers/config_manager.gd`
   - SceneManager: `res://scripts/managers/scene_manager.gd`
   - SaveManager: `res://scripts/managers/save_manager.gd`

---

## TDD开发步骤

### 📅 开发计划（4天）

#### Day 1: EventBus和ConfigManager
- 上午：EventBus测试和实现
- 下午：ConfigManager增强和测试

#### Day 2: GameManager核心
- 上午：GameManager测试框架
- 下午：GameManager基础实现

#### Day 3: SceneManager和SaveManager
- 上午：SceneManager测试和实现
- 下午：SaveManager测试和实现

#### Day 4:集成测试和优化
- 上午：系统集成测试
- 下午：性能优化和文档

---

## 测试实现指导

### 🧪 TDD开发流程（Red-Green-Refactor）

#### Red阶段：编写失败测试
1. 编写测试用例，验证功能需求
2. 确保测试失败（Red）
3. 验证测试逻辑正确

#### Green阶段：最小实现
1. 编写刚好让测试通过的最少代码
2. 不追求完美，只求通过
3. 确保所有测试通过（Green）

#### Refactor阶段：重构优化
1. 优化代码结构
2. 提高可读性和性能
3. 保持测试通过

### 📝 测试用例设计

#### 1. EventBus测试（test_event_bus.gd）

```gdscript
# 基础测试用例框架
class TestEventBus:
    extends "res://addons/gut/test.gd"

    var event_bus: EventBus

    func before_each():
        event_bus = EventBus.new()
        add_child_autofree(event_bus)

    # 测试事件订阅
    func test_event_subscription():
        # Arrange: 准备测试数据
        var received_data = null
        var test_callable = Callable(func(data): received_data = data)

        # Act: 执行操作
        event_bus.subscribe("test_event", test_callable)
        event_bus.emit_event("test_event", {"message": "hello"})

        # Assert: 验证结果
        assert_eq(received_data.message, "hello")

    # 测试事件取消订阅
    func test_event_unsubscription():
        # 实现测试逻辑
        pass

    # 测试事件优先级
    func test_event_priority():
        # 实现测试逻辑
        pass
```

#### 2. GameManager测试（test_game_manager.gd）

```gdscript
class TestGameManager:
    extends "res://addons/gut/test.gd"

    var game_manager: GameManager

    func before_each():
        game_manager = GameManager.new()
        add_child_autofree(game_manager)

    # 测试游戏初始化
    func test_game_initialization():
        # 测试游戏初始化流程
        pass

    # 测试管理器协调
    func test_manager_coordination():
        # 测试管理器间的协调
        pass

    # 测试游戏状态转换
    func test_game_state_transitions():
        # 测试游戏状态变化
        pass
```

### 🎯 关键测试场景

1. **EventBus测试场景**
   - 事件订阅和分发
   - 多订阅者优先级
   - 事件参数传递
   - 异步事件处理

2. **ConfigManager测试场景**
   - 配置文件加载
   - 默认值处理
   - 配置验证
   - 热重载功能

3. **GameManager测试场景**
   - 初始化流程
   - 状态转换
   - 错误处理
   - 管理器协调

4. **SceneManager测试场景**
   - 场景加载
   - 异步加载
   - 场景切换
   - 进度管理

5. **SaveManager测试场景**
   - 存档保存
   - 存档加载
   - 数据验证
   - 自动保存

---

## 代码实现指导

### 📋 核心方法签名

#### 1. GameManager实现

```gdscript
# game_manager.gd
extends Node

signal game_initialized
signal game_state_changed(new_state: GameState)
signal shutdown_requested

enum GameState {
    INITIALIZING,
    RUNNING,
    PAUSED,
    GAME_OVER,
    SHUTTING_DOWN
}

# 核心方法签名（不要实现具体代码）
func initialize_game() -> void:
    pass

func shutdown_game() -> void:
    pass

func pause_game() -> void:
    pass

func resume_game() -> void:
    pass

func get_game_state() -> GameState:
    pass

func _ready() -> void:
    pass

func _process(delta: float) -> void:
    pass
```

#### 2. EventBus实现

```gdscript
# event_bus.gd
extends Node

# 核心方法签名
func subscribe(event_name: String, callable: Callable, priority: int = 0) -> void:
    pass

func unsubscribe(event_name: String, callable: Callable) -> void:
    pass

func emit_event(event_name: String, data: Dictionary = {}) -> void:
    pass

func emit_event_deferred(event_name: String, data: Dictionary = {}) -> void:
    pass

func clear_all_subscriptions() -> void:
    pass

func get_subscription_count(event_name: String) -> int:
    pass
```

#### 3. ConfigManager增强

```gdscript
# config_manager.gd (基于现有实现增强)
extends Node

signal config_loaded(section: String, key: String)
signal config_saved(section: String, key: String)
signal config_error(error_message: String)

# 核心方法签名（新增方法）
func load_config(file_path: String) -> bool:
    pass

func save_config(file_path: String) -> bool:
    pass

func validate_config() -> bool:
    pass

func reload_config() -> bool:
    pass
```

#### 4. SceneManager实现

```gdscript
# scene_manager.gd
extends Node

signal scene_started_loading(scene_path: String)
signal scene_finished_loading(scene_path: String)
signal scene_loaded(scene_path: String)
signal scene_unloaded(scene_path: String)

enum SceneTransition {
    NONE,
    FADE,
    SLIDE,
    INSTANT
}

# 核心方法签名
func load_scene(scene_path: String, transition: SceneTransition = SceneTransition.INSTANT) -> void:
    pass

func load_scene_async(scene_path: String) -> void:
    pass

func unload_scene(scene_path: String) -> void:
    pass

func get_current_scene() -> Node:
    pass

func get_loading_progress() -> float:
    pass

func preload_scene(scene_path: String) -> Resource:
    pass
```

#### 5. SaveManager实现

```gdscript
# save_manager.gd
extends Node

signal save_completed(slot_index: int, success: bool)
signal load_completed(slot_index: int, success: bool)
signal save_error(error_message: String)

# 核心方法签名
func save_game(slot_index: int, data: Dictionary) -> bool:
    pass

func load_game(slot_index: int) -> Dictionary:
    pass

func delete_save(slot_index: int) -> bool:
    pass

func get_save_info(slot_index: int) -> Dictionary:
    pass

func get_all_save_slots() -> Array[Dictionary]:
    pass

func validate_save_data(data: Dictionary) -> bool:
    pass

func auto_save() -> bool:
    pass
```

### 🎮 实现步骤

#### 步骤1：创建基础文件结构
1. 创建 `scripts/managers/` 目录
2. 创建 `test/unit/`、`test/integration/`、`test/e2e/` 目录
3. 创建所有管理器脚本文件（只包含方法签名）
4. 创建所有测试文件（只包含测试框架）

#### 步骤2：TDD实现EventBus
1. 编写 `test_event_bus.gd` 中的测试用例
2. 运行测试，确保失败（Red阶段）
3. 实现 `event_bus.gd` 中的方法（Green阶段）
4. 重构优化代码（Refactor阶段）
5. 重复直到所有测试通过

#### 步骤3：增强ConfigManager
1. 分析现有的 `scripts/managers/config_manager.gd`
2. 编写缺失功能的测试用例
3. 实现新功能
4. 验证所有测试通过

#### 步骤4：实现GameManager
1. 编写GameManager测试用例
2. 实现GameManager核心功能
3. 测试与其他管理器的协调
4. 验证游戏生命周期管理

#### 步骤5：实现SceneManager
1. 编写场景管理测试
2. 实现场景加载功能
3. 实现异步加载
4. 添加进度管理

#### 步骤6：实现SaveManager
1. 编写存档系统测试
2. 实现数据序列化
3. 实现存档验证
4. 添加自动保存

---

## 集成测试

### 🔗 管理器协调测试

```gdscript
# test_manager_coordination.gd
class TestManagerCoordination:
    extends "res://addons/gut/test.gd"

    func test_game_startup_flow():
        # 测试游戏启动时的管理器初始化顺序
        pass

    func test_event_based_communication():
        # 测试基于事件的系统通信
        pass

    func test_error_propagation():
        # 测试错误处理和传播
        pass
```

### 🌊 事件流测试

```gdscript
# test_event_flow.gd
class TestEventFlow:
    extends "res://addons/gut/test.gd"

    func test_scene_change_event_flow():
        # 测试场景切换时的事件流
        pass

    func test_save_load_event_flow():
        # 测试保存加载时的事件流
        pass
```

### 🎯 端到端测试

```gdscript
# test_game_lifecycle.gd
class TestGameLifecycle:
    extends "res://addons/gut/test.gd"

    func test_complete_game_session():
        # 测试完整的游戏会话
        # 启动 → 场景加载 → 保存 → 退出
        pass
```

---

## 部署配置

### ⚙️ AutoLoad配置步骤

1. **打开Godot编辑器**
2. **项目 → 项目设置 → AutoLoad**
3. **添加以下路径**：
   - Name: `GameManager`, Path: `res://scripts/managers/game_manager.gd`
   - Name: `EventBus`, Path: `res://scripts/managers/event_bus.gd`
   - Name: `ConfigManager`, Path: `res://scripts/managers/config_manager.gd`
   - Name: `SceneManager`, Path: `res://scripts/managers/scene_manager.gd`
   - Name: `SaveManager`, Path: `res://scripts/managers/save_manager.gd`
4. **确保所有单例都勾选"Enable"**
5. **调整顺序**：GameManager应该在最前面

### 📄 配置文件设置

创建 `configs/game_config.cfg`：

```ini
[game]
max_save_slots=3
auto_save_interval=60.0
default_scene="res://scenes/main_menu.tscn"
debug_mode=false

[scene]
transition_duration=0.5
loading_screen="res://scenes/ui/loading_screen.tscn"
preload_scenes=""

[save]
save_directory="user://saves/"
save_format="json"
compression_enabled=true
backup_count=3

[event]
max_event_queue_size=1000
event_timeout=5.0
```

### 🔍 测试运行配置

1. **安装GUT测试框架**（如果尚未安装）
2. **在项目设置中添加测试场景**
3. **配置测试运行快捷键**
4. **设置CI/CD测试流水线**（可选）

---

## 验收检查清单

### ✅ 功能验收标准

- [ ] 游戏启动时，GameManager初始化所有子系统
- [ ] 场景切换时，GameManager协调资源加载和释放
- [ ] 触发游戏事件时，EventBus正确分发事件到订阅者
- [ ] 需要配置参数时，ConfigManager从文件加载配置
- [ ] 保存进度时，GameManager调用相应的管理器保存数据
- [ ] 所有管理器使用AutoLoad机制，全局可访问
- [ ] 系统间通过事件解耦，避免直接依赖

### 🧪 测试质量检查

- [ ] 单元测试覆盖率达到70%以上
- [ ] 集成测试覆盖关键流程
- [ ] 端到端测试验证完整场景
- [ ] 所有测试用例通过
- [ ] 测试文档完整

### 💻 代码质量检查

- [ ] 所有管理器正确注册为AutoLoad
- [ ] 系统间无直接依赖，通过EventBus通信
- [ ] 事件命名规范统一
- [ ] 错误处理完整
- [ ] 代码符合GDScript风格指南
- [ ] 类型提示完整
- [ ] 性能基准达标（60FPS）

### 📚 文档完整性

- [ ] 代码注释完整
- [ ] API文档生成
- [ ] 使用示例提供
- [ ] 配置说明详细
- [ ] 故障排除指南

### 🚀 部署就绪检查

- [ ] AutoLoad配置正确
- [ ] 配置文件存在且格式正确
- [ ] 测试场景可正常运行
- [ ] 无控制台错误或警告
- [ ] 性能符合预期

---

## 📖 扩展阅读

### 相关文档
- [GDScript官方风格指南](https://docs.godotengine.org/en/stable/tutorials/scripting/gdscript/gdscript_styleguide.html)
- [GUT测试框架文档](https://github.com/bitwes/Gut)
- [Godot最佳实践](https://docs.godotengine.org/en/stable/tutorials/best_practices/index.html)

### 模板和示例
- 测试用例模板位于 `test/templates/`
- 管理器实现示例位于 `examples/managers/`
- 配置文件示例位于 `examples/configs/`

---

**开发完成后，请更新backlog.md中此Story的状态为"已完成"**

**Happy Coding! 🎮✨**